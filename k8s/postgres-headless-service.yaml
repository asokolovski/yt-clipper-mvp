apiVersion: v1
kind: Service
metadata:
  name: database 
  namespace: yt-clipper
spec:
  type: ClusterIP
  clusterIP: None # This makes it a "headless" service
  selector:
    app: postgres-pod
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
